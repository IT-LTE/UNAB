package cl.lte;

// Clase principal con main limpio
public class App {
    public static void main(String[] args) {
        // Solo inicia el simulador, sin logica de negocio
        MenuSimulador menu = new MenuSimulador();
        menu.iniciar();
    }
}
